# System trust contract (minimal, practical)

This repo treats server governance like production:
- baseline expectations are explicit
- checks run in CI (demo) and can be run on real hosts (production, guarded)
- evidence artifacts are deterministic and easy to review

The point is to make operations safer without slowing delivery.
